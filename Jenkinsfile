pipeline {
  agent any

  stages {
    stage('build') {
      steps { echo 'build app...' }
    }

    stage('test') {
      steps {
        echo 'testing app...'
      }
    }

    stage('deploy') {
      steps {
        echp 'deplying app...'
      }
    }
  }
}
